---
phase: 08-hash-ring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/coci/hashing/hash_ring.py
  - src/coci/hashing/__init__.py
autonomous: true
requirements:
  - HR-01
  - HR-02
  - HR-03
  - HR-04

must_haves:
  truths:
    - "Hash ring maps shard positions to GPU nodes correctly"
    - "100 virtual nodes per physical GPU for load balancing"
    - "SHA256 hash function normalized to [0.0, 1.0) range"
    - "Shard owner lookup returns nearest clockwise node"
  artifacts:
    - path: "src/coci/hashing/hash_ring.py"
      provides: "HashRing and ShardManager classes"
      min_lines: 80
    - path: "src/coci/hashing/__init__.py"
      provides: "Module exports"
      exports: ["HashRing", "ShardManager", "create_hash_ring", "create_shard_manager"]
  key_links:
    - from: "src/coci/hashing/hash_ring.py"
      to: "Phase 9 (Shard Manager)"
      via: "ShardManager class"
      pattern: "class ShardManager"
    - from: "src/coci/hashing/hash_ring.py"
      to: "Phase 10 (Fault Detector)"
      via: "HashRing used by fault detection"
      pattern: "get_node.*shard"
---

<objective>
Implement consistent hashing with virtual nodes for shard management.

Purpose: Enable efficient shard-to-node mapping with load balancing and minimal redistribution on node failure.

Output: HashRing class with virtual nodes, ShardManager for lifecycle tracking
</objective>

<execution_context>
@C:/Users/potta/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/potta/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md (Phase 8: Hash Ring)
@.planning/REQUIREMENTS.md (HR-01 through HR-04)

# Implementation reference
@HASH_RING.md (Sections 3.1, 3.2 - Hash Ring Manager, Checkpoint Shard Manager)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify HashRing implementation</name>
  <files>src/coci/hashing/hash_ring.py</files>
  <behavior>
    - HashRing accepts virtual_nodes_per_physical parameter (default 100)
    - add_node() creates virtual nodes with SHA256 hash positions
    - get_node(key) returns nearest clockwise node via bisect
    - remove_node() cleans up all virtual nodes for a physical node
  </behavior>
  <action>
Verify existing HashRing implementation:
1. HashRing class with virtual_nodes_per_physical parameter
2. SHA256 hash function normalized to [0.0, 1.0)
3. get_node() uses bisect.bisect_right for nearest clockwise
4. Virtual node cleanup on node removal
  </action>
  <verify>
    <automated>python -c "from src.coci.hashing import HashRing; r=HashRing(100); r.add_node('gpu-0'); r.add_node('gpu-1'); print('OK:', r.get_node('shard_1'))"</automated>
  </verify>
  <done>HashRing returns valid node for any shard key</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify ShardManager integration</name>
  <files>src/coci/hashing/hash_ring.py</files>
  <behavior>
    - ShardManager accepts hash_ring in constructor
    - register_shard() assigns owner via hash_ring.get_node()
    - get_owner() returns current owner node
    - reassign_shard() updates owner after failure
  </behavior>
  <action>
Verify ShardManager with HashRing integration:
1. ShardManager constructor takes hash_ring reference
2. register_shard() uses hash_ring.get_node() for initial assignment
3. get_owner() returns shard's current owner
4. reassign_shard() changes owner and marks as ORPHANED if previously CACHED
  </action>
  <verify>
    <automated>python -c "from src.coci.hashing import HashRing, ShardManager; r=HashRing(100); r.add_node('gpu-0'); sm=ShardManager(r); sm.register_shard('shard_1', 1, 1000, '/storage/shard_1.pt'); print('OK:', sm.get_owner('shard_1'))"</automated>
  </verify>
  <done>ShardManager correctly assigns shards via hash ring</done>
</task>

<task type="auto">
  <name>Task 3: Create module exports</name>
  <files>src/coci/hashing/__init__.py</files>
  <action>
Add factory functions to __init__.py:
- create_hash_ring(node_ids, virtual_nodes=100)
- create_shard_manager(hash_ring, cache_dir='/nvme/cache')

Ensure all classes are exported properly.
  </action>
  <verify>
    <automated>python -c "from src.coci.hashing import create_hash_ring, create_shard_manager; print('OK')"</automated>
  </verify>
  <done>Module provides clean factory functions</done>
</task>

</tasks>

<verification>
All tests pass:
- HashRing: 4 nodes, 100 virtual nodes each, 128 shards distributed evenly
- ShardManager: 32 shards registered with correct owners
- Imports work without errors
</verification>

<success_criteria>
- Hash ring maps shard positions to GPU nodes correctly
- 100 virtual nodes per physical GPU for load balancing
- SHA256 hash function normalized to [0.0, 1.0) range
- Shard owner lookup returns nearest clockwise node
</success_criteria>

<output>
After completion, create `.planning/phases/08-hash-ring/08-01-SUMMARY.md`
</output>