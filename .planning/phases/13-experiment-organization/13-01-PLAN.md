---
phase: 13-experiment-organization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - experiment_results/index/migration_script.py
  - experiment_results/index/experiment_register.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "All 20 experiments are parsed and categorized by mode, framework, and date"
    - "New naming convention is defined and documented"
    - "Migration script can perform dry-run without modifying files"
  artifacts:
    - path: "experiment_results/index/migration_script.py"
      provides: "Script to migrate experiments to new naming convention"
      min_lines: 50
    - path: "experiment_results/index/experiment_register.json"
      provides: "Mapping from old names to new names"
      contains: "old_name, new_name"
  key_links:
    - from: "migration_script.py"
      to: "experiment_summaries.jsonl"
      via: "reads metadata to determine experiment attributes"
      pattern: "experiment_summaries.jsonl"
---

<objective>
Analyze current experiment structure and create migration infrastructure for reorganizing experiment results.

Purpose: Create a clear mapping from the current timestamp-based experiment names to a new organized naming convention that makes experiments easy to find and compare.

Output: Migration script and experiment register mapping old names to new names.
</objective>

<execution_context>
@C:/Users/potta/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/potta/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Sai/HPC/project/adaptive-ring-checkpointing/.planning/phases/13-experiment-organization/13-experiment-organization-CONTEXT.md
@D:/Sai/HPC/project/adaptive-ring-checkpointing/experiment_results/experiment_summaries.jsonl

Current experiment directories use timestamp-based naming (e.g., `faceforensics_convergence_20260419_104954`) which makes it hard to:
- Quickly identify the training mode (epoch vs convergence vs hash-ring)
- Identify the framework used (vanilla PyTorch, DeepSpeed, FSDP, Lightning, HuggingFace)
- Compare similar experiments
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parse experiment metadata and design naming convention</name>
  <files>experiment_results/index/experiment_register.json</files>
  <action>
    Read experiment_summaries.jsonl to extract metadata for all 20 experiments.
    
    For each experiment, extract:
    - experiment_name (old name)
    - training_mode (epoch, convergence, hash-ring-epoch, convergence-hash-ring, etc.)
    - framework (vanilla, deepspeed, fsdp, lightning, hf_trainer, wandb_artifacts)
    - timestamp (from experiment_name or timestamp field)
    
    Design new naming convention: `{mode}_{framework}_{date}_{time}`
    Examples:
    - epoch_vanilla_20260419_101309
    - convergence_vanilla_20260419_104954
    - convergence_hashring_20260419_113143
    - epoch_hashring_20260419_112016
    - deepspeed_vanilla_20260430_175303
    
    Create experiment_register.json with mapping:
    ```json
    [
      {
        "old_name": "faceforensics_epoch_20260419_101309",
        "new_name": "epoch_vanilla_20260419_101309",
        "mode": "epoch",
        "framework": "vanilla",
        "date": "20260419",
        "time": "101309"
      },
      ...
    ]
    ```
    
    Note: Determine framework from experiment_name pattern:
    - Contains "deepspeed" → deepspeed
    - Contains "fsdp" → fsdp
    - Contains "pytorch_lightning" → lightning
    - Contains "hf_trainer" → hf_trainer
    - Contains "wandb_artifacts" → wandb
    - Default → vanilla
    
    Note: Determine mode from training_mode field (in experiment_summaries.jsonl):
    - "epoch" → epoch
    - "hash-ring-epoch" → epoch_hashring
    - "convergence" → convergence
    - "convergence-hash-ring" → convergence_hashring
  </action>
  <verify>
    <automated>python -c "import json; data = json.load(open('experiment_results/index/experiment_register.json')); print(f'Mapped {len(data)} experiments'); assert len(data) == 20, 'Expected 20 experiments'; assert all('old_name' in e and 'new_name' in e for e in data)"</automated>
  </verify>
  <done>experiment_register.json created with all 20 experiments mapped from old timestamp-based names to new organized names with mode, framework, date, time fields</done>
</task>

<task type="auto">
  <name>Task 2: Create migration script with dry-run capability</name>
  <files>experiment_results/index/migration_script.py</files>
  <action>
    Create a Python migration script that:
    
    1. Reads experiment_register.json for old→new name mappings
    2. Supports --dry-run flag (default: True for safety)
    3. For each experiment:
       - Validates old directory exists
       - Creates new directory name
       - If not dry-run: renames directory (or copies + validates + deletes old)
    4. Updates experiment_summaries.jsonl with new experiment_name values
    5. Creates backup of experiment_summaries.jsonl before modification
    
    Implementation notes:
    - Use os.rename() for same-filesystem moves (fast)
    - Fallback to shutil.copytree() + os.remove() for cross-device
    - On Windows, handle path separators correctly
    - Preserve all files within each experiment directory
    - Print what would be done during dry-run
    
    Script usage:
    ```bash
    # Dry run (see what would happen)
    python experiment_results/index/migration_script.py --dry-run
    
    # Execute migration
    python experiment_results/index/migration_script.py --execute
    ```
    
    After migration, update experiment_summaries.jsonl:
    - Read each line, parse JSON
    - Update "experiment_name" field to new name
    - Write to temp file, then replace original
  </action>
  <verify>
    <automated>python experiment_results/index/migration_script.py --dry-run 2>&1 | Select-String "Would rename" | Measure-Object | Select-Object Count</automated>
  </verify>
  <done>Migration script created with dry-run capability, successfully parses register, and can rename directories and update experiment_summaries.jsonl</done>
</task>

</tasks>

<verification>
- experiment_register.json contains all 20 experiments with correct new names
- migration_script.py --dry-run shows 20 renames without errors
- New naming convention is clear: {mode}_{framework}_{date}_{time}
</verification>

<success_criteria>
- 20 experiments parsed and mapped in experiment_register.json
- New naming convention clearly identifies mode and framework
- Migration script works in dry-run mode without errors
- Script can be executed to perform actual migration
</success_criteria>

<output>
After completion, create `.planning/phases/13-experiment-organization/13-01-SUMMARY.md`
</output>
